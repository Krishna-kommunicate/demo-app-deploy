version: 0.2

phases:
  install:
    commands:
      - npm install
  # Remove the build phase if you don’t need it
  # build:
  #   commands:
  #     - npm run build

artifacts:
  files:
    - '**/*'